<h1>Sitio Web</h1>

<section class="card">
  <section class="card-block">
    <%= render 'form', site: @site %>
  </section>
</section>

<%= link_to 'Ver', @site %> |
<%= link_to 'Volver', sites_path %>