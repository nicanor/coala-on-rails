<%= form_for(site) do |f| %>
  <% if site.errors.any? %>
    <article class="alert alert-danger">
      <%= site.errors.count %>
      error(es) no permiten guardar este sitio web:
     
      <ul>
        <% site.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </article>
  <% end %>

  <fieldset class="form-group">
    <%= f.label :code, 'Código' %>
    <%= f.text_field :code,
        class: 'form-control' %>
  </fieldset>

  <fieldset class="form-group">
    <%= f.label :name, 'Nombre' %>
    <%= f.text_field :name,
        class: 'form-control' %>
  </fieldset>

  <fieldset class="form-group">
    <%= f.label :description,
        'Descripción' %>
    <%= f.text_field :description,
        class: 'form-control' %>
  </fieldset>

  <div class="actions">
    <%= f.submit 'Guardar',
        class: 'btn btn-primary' %>
  </div>
<% end %>